# Para asignar un nuevo valor a una variable existente, se utiliza el operador de asignación "=" para asignar el valor deseado.
var = 1
print(var)
var = var + 1
print(var)


#La primer línea del código crea una nueva variable llamada var y le asigna el valor de 1.

#La sentencia se lee de la siguiente manera: asigna el valor de 1 a una variable llamada var.

#De manera más corta: asigna 1 a var.


var = 100
var = 200 + 300
print(var)

# se crea la variable var y se le asigna un valor de 100.
# Luego, a la misma variable se le asigna un nuevo valor: el resultado de sumar 200 a 300, que es 500.



